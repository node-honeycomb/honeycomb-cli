#!/usr/bin/env node

'use strict';
const randomatic = require('randomatic');

function genToken(token) {
  /*eslint-disable */
  console.log('-------------');
  console.log('token:', randomatic('*', 64));
  /* eslint-enable */
}

genToken();
